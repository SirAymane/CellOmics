export class Province {
  static getProvince() {
  }
	// Private fields are only accessible within this class
	id: number;
	

	// Constructor initializes the private fields with the provided arguments
	constructor(id: number) {
		this.id = id;
	}


}
